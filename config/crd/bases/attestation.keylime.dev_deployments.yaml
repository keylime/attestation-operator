---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: deployments.attestation.keylime.dev
spec:
  group: attestation.keylime.dev
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the deployments API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DeploymentSpec defines the desired state of Deployment
            properties:
              enabled:
                description: Enabled is a boolean that allows to specify if controller
                  based deployment is enabled
                type: boolean
              initGlobal:
                description: InitGlobal is a struct to define all information for
                  the initial deployment is enabled
                properties:
                  imageInfo:
                    description: ImageInfo defines global information for initial
                      image in initial deployment task
                    properties:
                      imageRepositoryPath:
                        description: ImageRepositoryPath is a string to specify where
                          to download images
                        type: string
                      imageTag:
                        description: ImageTag is a string to specify which image tag
                          to download
                        type: string
                    type: object
                type: object
              nodeInfo:
                description: 'NodeInfo is a struct to define all information for nodes
                  deployed: - tenant - registrar - verifier'
                properties:
                  registrarImageInfo:
                    description: ImageInfo defines global information for initial
                      image in initial deployment task
                    properties:
                      imageRepositoryPath:
                        description: ImageRepositoryPath is a string to specify where
                          to download images
                        type: string
                      imageTag:
                        description: ImageTag is a string to specify which image tag
                          to download
                        type: string
                    type: object
                  tenantImageInfo:
                    description: ImageInfo defines global information for initial
                      image in initial deployment task
                    properties:
                      imageRepositoryPath:
                        description: ImageRepositoryPath is a string to specify where
                          to download images
                        type: string
                      imageTag:
                        description: ImageTag is a string to specify which image tag
                          to download
                        type: string
                    type: object
                  verifierImageInfo:
                    description: ImageInfo defines global information for initial
                      image in initial deployment task
                    properties:
                      imageRepositoryPath:
                        description: ImageRepositoryPath is a string to specify where
                          to download images
                        type: string
                      imageTag:
                        description: ImageTag is a string to specify which image tag
                          to download
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: DeploymentStatus defines the observed state of Deployment
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
